@model IEnumerable<LaEsperanza.WEB.Models.Clasification>

@{
    ViewData["Title"] = "Index";
}

<h2>Clasificaciones <a class="btn btn-warning" asp-action="Create">Agregar</a></h2>


<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ItemType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ItemType)
            </td>
            <td>
                <a class="btn btn-success" asp-action="Edit" asp-route-id="@item.ClasificationId">Editar</a> |
                <a class="btn btn-dark" asp-action="Details" asp-route-id="@item.ClasificationId">Detalles</a> |
                <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.ClasificationId">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
